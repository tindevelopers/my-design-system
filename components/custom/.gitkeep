# Custom Components

Place custom-built components in this directory.

Each component should:
- Use TypeScript
- Export a named component
- Include TypeScript interface for props
- Follow the project's coding standards (see .cursorrules)
